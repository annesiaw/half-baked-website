{
  "name": "half-baked-website",
  "version": "1.0.0",
  "description": "Half Baked - Sweet Treats, Anytime, Anywhere",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
